"""examshell — a local emulation of the 42 exam shell (grademe-style).

The exam *content* (subjects + reference solutions) lives in the
``exam_rank_0X/`` folders. This package is only the simulator: it renders
subjects, scaffolds a student ``rendu/`` workspace and grades the student's
code against the reference solution used as an oracle.
"""
